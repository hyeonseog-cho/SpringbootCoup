spring:
  # security:
  #   user:
  #     name: user
  #     password: testpw
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: myuser
    url: jdbc:mysql://<EC2_INSTANCE_PUBLIC_IP>:3306/mydatabase
    password: mypassword
  jpa:
    hibernate:
      ddl-auto: update

  
  devtools:
    restart:
      # 로깅시 재시작 방지
      exclude: logs/**
  
  output:
    ansi:
      enabled: always

server:
  port: 5000
  # servlet:
  #   context-path: /index


